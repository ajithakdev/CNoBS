/*
 * Practice: Print a left-aligned triangle of stars. | Difficulty: easy
 *
 * Spec:
 *   n = 4 ->
 *     *
 *     **
 *     ***
 *     ****
 *
 * Run: gcc print-triangle.c && ./a.out
 *
 * EXPECTED TEST OUTPUT (when solved, n=4):
 *   *
 *   **
 *   ***
 *   ****
 */

#include <stdio.h>

void triangle(int n) {
    /* TODO: nested loops. Outer = rows, inner = stars per row. */
    (void)n;
}

int main(void) {
    triangle(4);
    return 0;
}
